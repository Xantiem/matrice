#include <stdio.h>

#include "matrix.h"

int matrix(int arr, int size) {

    return sizeof(arr) / size;
}

int matrix_gen (unsigned short int creation_size) {

}

int main() {
    int size[] = {2,2};

    

    printf("%d", length);

    return 0;
}